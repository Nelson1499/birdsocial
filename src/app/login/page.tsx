import { AuthLoginServer } from "@/components/login/login-server"

export default function Login () {
  return (
    <main className="sm:w-[600px]  w-screen h-screen flex items-center justify-center">
      <AuthLoginServer />
    </main>
  )
}
